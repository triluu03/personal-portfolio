import { Box, Typography } from '@mui/material'

const NavBar = () => {
    return (
        <Box sx={{ width: '100vw', height: 95 }}>
            <Typography>NavBar</Typography>
        </Box>
    )
}

export default NavBar
